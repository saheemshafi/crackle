import Container from "@/components/Container";
import { FC } from "react";

interface pageProps {}

const PopularMoviesPage: FC<pageProps> = ({}) => {
  return <Container>page</Container>;
};

export default PopularMoviesPage;
