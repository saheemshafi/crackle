import Container from "@/components/Container";
import { FC } from "react";

interface pageProps {}

const NowPlayingPage: FC<pageProps> = ({}) => {
  return <Container>page</Container>;
};

export default NowPlayingPage;
