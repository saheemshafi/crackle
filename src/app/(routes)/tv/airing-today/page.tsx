import Container from "@/components/Container";
import { FC } from "react";

interface pageProps {}

const AiringTodayPage: FC<pageProps> = ({}) => {
  return <Container>page</Container>;
};

export default AiringTodayPage;
