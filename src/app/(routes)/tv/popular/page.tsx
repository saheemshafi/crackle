import Container from "@/components/Container";
import { FC } from "react";

interface pageProps {}

const PopularSeriesPage: FC<pageProps> = ({}) => {
  return <Container>page</Container>;
};

export default PopularSeriesPage;
