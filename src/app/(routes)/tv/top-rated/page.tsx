import Container from "@/components/Container";
import { FC } from "react";

interface pageProps {}

const TopRatedSeriesPage: FC<pageProps> = ({}) => {
  return <Container>page</Container>;
};

export default TopRatedSeriesPage;
