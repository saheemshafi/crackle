import Container from "@/components/Container";
import { FC } from "react";

interface pageProps {}

const OnTvPage: FC<pageProps> = ({}) => {
  return <Container>page</Container>;
};

export default OnTvPage;
