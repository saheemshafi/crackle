import Container from "@/components/Container";
import { FC } from "react";

interface pageProps {}

const UpcomingMoviesPage: FC<pageProps> = ({}) => {
  return <Container>page</Container>;
};

export default UpcomingMoviesPage;
